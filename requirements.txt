# Core Dependencies
torch>=2.0.0
torchaudio>=2.0.0
# qwen-tts currently requires transformers==4.57.3
transformers==4.57.3
accelerate>=0.25.0
qwen-tts==0.1.1
# qwen-asr metadata currently conflicts with qwen-tts (transformers pin mismatch).
# Install qwen-asr manually after this file:
#   uv pip install --no-deps qwen-asr==0.0.6
silero-vad>=5.1.2
torchcodec>=0.1.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.11.0

# Video Processing
# FFmpeg must be installed separately via system package manager

# API & Web
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
gradio>=4.0.0

# Task Queue
celery>=5.3.0
redis>=5.0.0

# Utilities
python-dotenv>=1.0.0
typer>=0.9.0
rich>=13.0.0
tqdm>=4.66.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HuggingFace
huggingface-hub>=0.20.0

# Optional: FlashAttention 2 for better GPU performance
# Install separately (do not add pip flags inline in requirements):
#   pip install --no-build-isolation flash-attn
# If no wheel exists for your Python version (common on Python 3.13),
# create a Python 3.12 env and install there.
# To disable warning without installing flash-attn:
#   FLASH_ATTENTION=false

# Optional: For translation
# translate>=3.5.0
# deep-translator>=1.9.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
